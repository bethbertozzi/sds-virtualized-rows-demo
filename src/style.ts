import { styled } from "@mui/material/styles";
import { TableHeader } from "czifui";

export const StyledTableHeader = styled(TableHeader)`
  background: lightgray;
  margin: 0;
  position: sticky;
  top: 0;
`;
